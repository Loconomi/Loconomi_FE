import styled from 'styled-components';

const HomeScreen = () => {
  return (
    <HomeScreenContainer>
    </HomeScreenContainer>
  );
};

export default HomeScreen;

const HomeScreenContainer = styled.div`
    overflow-y: auto; // 이거 있어야 됨
`;
